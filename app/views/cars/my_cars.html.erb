<% @cars.each do |car| %>
  <h1><%= link_to "#{car.brand} - #{car.model}", car_path(car) %></h1>
  <h4>Model <%= car.year %></h4>
  <h4>Category: <%= car.category %></h4>
  <p><%= car.description %></p>
  <h5>City: <%= car.city %></h5>
  <br><br>
<% end %>
